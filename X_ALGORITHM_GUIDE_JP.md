# Xアルゴリズムを解析してわかった「ポストを伸ばす」新常識

Xの内部コードを解析した結果、私たちが思っている以上に**「評価の重み」**に差があることが判明しました。

結論から言うと、「いいね」を狙うだけの運用はもう古いかもしれません。

---

## 📊 アルゴリズムの仕組み

このリポジトリに含まれるFor Youフィードのアルゴリズムは、**Phoenix**と呼ばれるGrokベースのTransformerモデルを使用して、各ポストのエンゲージメント確率を予測し、重み付けスコアを計算します。

### 予測される行動タイプ

コード解析により、以下の行動が予測・評価されています：

| 行動タイプ | 内部名 | 評価 |
|-----------|--------|------|
| いいね | `favorite_score` | ✅ 正の重み |
| リプライ（返信） | `reply_score` | ✅ 正の重み（高評価） |
| リポスト | `retweet_score` | ✅ 正の重み（高評価） |
| 引用リポスト | `quote_score` | ✅ 正の重み |
| クリック | `click_score` | ✅ 正の重み |
| プロフィールクリック | `profile_click_score` | ✅ 正の重み |
| 動画視聴 | `vqv_score` | ✅ 正の重み |
| 画像拡大 | `photo_expand_score` | ✅ 正の重み |
| シェア | `share_score` | ✅ 正の重み |
| DMでシェア | `share_via_dm_score` | ✅ 正の重み |
| リンクコピーでシェア | `share_via_copy_link_score` | ✅ 正の重み |
| 滞在時間 | `dwell_score` / `dwell_time` | ✅ 正の重み |
| フォロー | `follow_author_score` | ✅ 正の重み |
| 興味なし | `not_interested_score` | ❌ **負の重み** |
| ブロック | `block_author_score` | ❌ **負の重み** |
| ミュート | `mute_author_score` | ❌ **負の重み** |
| 報告 | `report_score` | ❌ **負の重み** |

> 📍 ソースコード: [`home-mixer/scorers/weighted_scorer.rs`](home-mixer/scorers/weighted_scorer.rs)

---

## 1. 🗣️ 「リプライ」は最強のブースト

アルゴリズムは、ユーザー同士の「会話」を最も重視しています。

### 解析結果
- リプライ（返信）は、**いいねの数倍のスコア**で評価される仕組みになっています。
- コード内の `reply_score` は独立した重み（`REPLY_WEIGHT`）が設定されており、会話を促進するポストが優先的に表示されます。

### 攻略法
- 「どう思いますか？」と問いかけたり、あえてツッコミどころを作るなど、**「返信のハードルを下げる」**ことがリーチ爆発の鍵です。
- 質問形式のポストや、意見を求める投稿が効果的です。

---

## 2. 🔄 「リポスト」は信頼の証

単なる拡散だけでなく、アルゴリズムは「誰が広めたか」も見ています。

### 解析結果
- リポスト（`retweet_score`）や引用リポスト（`quote_score`）は、フォロワー外（おすすめフィード）へ表示させるための最大の加点要素です。
- さらにシェア関連のスコア（`share_score`, `share_via_dm_score`, `share_via_copy_link_score`）も評価されます。

### 攻略法
- 「後で見返したい」と思われる保存版の情報や、強い共感を生むエピソードを投稿しましょう。
- 有益な情報やインフォグラフィックは、リポストされやすい傾向にあります。

---

## 3. ⏱️ 「滞在時間」という隠れた指標

実は、画面を止めて読んでいる時間もスコアに影響しています。

### 解析結果
- `dwell_score`（滞在時間）という項目があり、じっくり読まれるポストは「質の高いコンテンツ」と見なされます。
- 継続的な滞在時間（`dwell_time` / `CONT_DWELL_TIME_WEIGHT`）も別途評価されています。

### 攻略法
- 140文字ギリギリまで書く、魅力的な画像を添える、動画を活用するなど、**「ユーザーの指を止める」**工夫が不可欠です。
- スレッド形式で複数のポストに分けることも効果的です。

---

## 4. 🎬 動画コンテンツの優遇

動画投稿には特別な評価が適用されています。

### 解析結果
- 動画視聴スコア（`vqv_score`）には、**一定以上の長さの動画**（`MIN_VIDEO_DURATION_MS`を超える場合）に対して追加の重み（`VQV_WEIGHT`）が適用されます。
- 短すぎる動画は追加評価の対象外となります。

> 📍 ソースコード: [`home-mixer/scorers/weighted_scorer.rs`](home-mixer/scorers/weighted_scorer.rs) の `vqv_weight_eligibility` 関数

### 攻略法
- 短いクリップよりも、ある程度の長さがある動画を投稿しましょう。
- 視聴完了率が高い動画が評価されます。

---

## ⚠️ 注意：やりすぎは逆効果

### 過度な連投
短時間に投稿しすぎると、多様性を保つためのフィルター（**AuthorDiversityScorer**）が働き、2通目以降のスコアが下げられてしまいます。

```
同じ著者のポストが連続 → スコア減衰（decay_factor適用）
```

> 📍 ソースコード: [`home-mixer/scorers/author_diversity_scorer.rs`](home-mixer/scorers/author_diversity_scorer.rs)

具体的には：
- フィードに既に表示されている著者のポストは、**減衰係数（decay_factor）**によってスコアが下げられます。
- 最低限の表示（floor）は確保されますが、連投は効果が薄れます。

### ネガティブ反応
「興味なし」や「ブロック」をされると、スコアは致命的にマイナスされます。

- `not_interested_score` → 負の重み
- `block_author_score` → 負の重み
- `mute_author_score` → 負の重み
- `report_score` → 負の重み

これらのネガティブな反応は、ポジティブな反応を相殺してしまうため、炎上商法は長期的にはマイナスです。

---

## 📱 フィードへの表示ロジック

### イン・ネットワーク vs アウト・オブ・ネットワーク

アルゴリズムは2つのソースからポストを取得します：

| ソース | 説明 | 重み係数 |
|--------|------|----------|
| **イン・ネットワーク (Thunder)** | フォローしているアカウントのポスト | 標準 |
| **アウト・オブ・ネットワーク (Phoenix)** | ML検索で発見されたポスト | OON_WEIGHT_FACTOR適用 |

> 📍 ソースコード: [`home-mixer/scorers/oon_scorer.rs`](home-mixer/scorers/oon_scorer.rs)

フォロワー以外のフィードに表示されるためには、**より高いスコアが必要**です。

---

## 🚫 フィルターで除外されるケース

以下のポストはフィードから除外されます：

| フィルター | 除外条件 |
|-----------|----------|
| `AgeFilter` | 古すぎるポスト |
| `SelfpostFilter` | 自分自身のポスト |
| `AuthorSocialgraphFilter` | ブロック/ミュートしているアカウント |
| `MutedKeywordFilter` | ミュートキーワードを含むポスト |
| `PreviouslySeenPostsFilter` | 既に見たポスト |
| `PreviouslyServedPostsFilter` | 既にフィードに表示されたポスト |
| `RepostDeduplicationFilter` | 重複するリポスト |
| `VFFilter` | 削除済み/スパム/暴力的コンテンツ |

> 📍 ソースコード: [`home-mixer/filters/`](home-mixer/filters/)

---

## 🚀 今日からできるアクションプラン

1. **「いいね」より「リプライ」**が来る仕掛けを作る
   - 質問を投げかける
   - 意見を求める
   - ツッコミどころを残す

2. **「保存したくなる情報」**を1つ入れる
   - ノウハウ・Tips
   - 統計データ
   - チェックリスト

3. **動画や画像**を使って、滞在時間を1秒でも長くする
   - 読み応えのある長文
   - 魅力的なビジュアル
   - 動画コンテンツ

4. **連投を避ける**
   - 投稿間隔を空ける
   - 質より量を追求しない

5. **ネガティブ反応を避ける**
   - 炎上狙いは長期的にマイナス
   - 価値のあるコンテンツを提供

---

## 📖 技術的な詳細

詳しい技術的な説明は以下のドキュメントを参照してください：

- [README.md](README.md) - システムアーキテクチャの概要
- [phoenix/README.md](phoenix/README.md) - Phoenix MLモデルの詳細
- [home-mixer/](home-mixer/) - スコアリングとフィルタリングの実装

---

アルゴリズムを味方につけて、効率よくリーチを伸ばしていきましょう！

#Xアルゴリズム #SNS運用 #Twitterマーケティング #解析報告
